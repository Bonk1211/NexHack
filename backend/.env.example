# Supabase (§15) — get from `get_project_url` / `get_publishable_keys`
SUPABASE_URL=https://ffedvcwvvkdstzlvxxhr.supabase.co
SUPABASE_KEY=

# Model family — OPEN DECISION (§25). Pick one and LOCK. Two-tier: cheap per-step, larger per-run.
# Verify availability/pricing before locking. Left configurable on purpose.
LLM_PROVIDER=anthropic
LLM_MODEL_STEP=claude-haiku-4-5-20251001
LLM_MODEL_SYNTH=claude-opus-4-8
LLM_API_KEY=

# Determinism (§16)
RUN_SEED=1337

# WCAG target (§25 open decision) — default 2.2 AA
WCAG_VERSION=2.2

# Alerts (FR-4.3) — optional
SLACK_WEBHOOK_URL=
